# iPOS - Point de Vente Intelligent (Version Ultra-Light)

**iPOS** هو تطبيق نقطة بيع (POS) متطور، يعمل بالكامل داخل المتصفح (Client-side) باستخدام تقنية **IndexedDB**. هذه النسخة منقحة بالكامل لتعمل بأقصى سرعة وبدون أي ملفات زائدة.

## 🚀 البنية التقنية (النسخة المنقحة)

*   **Framework:** Next.js 14 (App Router)
*   **Base de Données:** IndexedDB (via Dexie.js) - محلي 100%
*   **Gestion d'état:** Zustand (مع Persistance في LocalStorage)
*   **UI:** ShadCN UI & Tailwind CSS (Dark Mode مثبت)
*   **Security:** خصوصية مطلقة، البيانات لا تغادر جهازك أبداً.

## 📁 هيكل المشروع النظيف (Clean Architecture)

- `src/app/`: المسارات والصفحات الرئيسية.
- `src/services/`: منطق الأعمال (Ventes, Stocks, Customers, Expenses).
- `src/lib/db.ts`: تعريف جداول قاعدة البيانات المحلية.
- `src/stores/`: إدارة حالة السلال المتعددة (Cart Store).
- `src/components/`: مكونات واجهة المستخدم (فقط المكونات الفعالة).

## ✨ المميزات الرئيسية

1.  **نظام البيع (POS):** واجهة سريعة تدعم السلال المتعددة والتحقق من المخزون لحظياً.
2.  **إدارة المخزون:** تتبع دقيق مع تنبيهات Stock Low وتاريخ انتهاء الصلاحية.
3.  **الديون والعملاء:** سجل كامل لكل عميل مع كشوفات حساب قابلة للطباعة (A4/Thermal).
4.  **نظام الخبز:** إدارة فريدة للطلبات اليومية المتكررة.
5.  **الأمان:** نظام نسخ احتياطي يدوي (Export/Import) لضمان سلامة بياناتك.

## 🗑️ تنظيف المشروع
تمت إزالة جميع بقايا نظام Supabase، ملفات Middleware، ونظام الصور الملحق لضمان بقاء الكود نظيفاً وسهلاً للصيانة.
