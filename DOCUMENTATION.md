# iPOS - Point de Vente Intelligent (Version Locale)

**iPOS** هو تطبيق نقطة بيع (POS) كامل، مصمم للعمل بشكل مستقل تماماً داخل المتصفح. يستخدم التطبيق تقنية **IndexedDB** لتخزين البيانات محلياً، مما يضمن السرعة والخصوصية والعمل بدون إنترنت.

## 🚀 البنية التقنية (النسخة المنقحة)

*   **Framework :** Next.js 14 (App Router)
*   **Base de Données :** IndexedDB (via Dexie.js) - *لا يوجد قاعدة بيانات خارجية*
*   **Gestion d'état :** Zustand (مع حفظ الحالة في LocalStorage)
*   **UI :** ShadCN UI & Tailwind CSS
*   **Architecture :** 100% Client-side / Offline-first

## 📁 هيكل المشروع النظيف

- `src/app/` : المسارات والصفحات (جميعها عامة ولا تتطلب تسجيل دخول).
- `src/services/` : منطق التعامل مع البيانات (IndexedDB).
- `src/lib/db.ts` : تعريف جداول قاعدة البيانات المحلية.
- `src/stores/` : إدارة حالة السلة والتطبيق.
- `src/components/` : مكونات واجهة المستخدم المعاد استخدامها.

## ✨ المميزات الرئيسية

1.  **البيع السريع :** واجهة متطورة تدعم السلال المتعددة (Brouillons).
2.  **إدارة المخزون :** تتبع دقيق للكميات، تنبيهات المخزون المنخفض، وتواريخ الانتهاء.
3.  **الديون والعملاء :** إدارة كاملة لحسابات العملاء والديون والمدفوعات.
4.  **المصاريف :** تتبع جميع مصاريف العمل وتصنيفها.
5.  **النسخ الاحتياطي :** تصدير واستيراد قاعدة البيانات بالكامل كملف JSON (للأمان الإضافي).
6.  **نظام الخبز :** وحدة خاصة لإدارة طلبات الخبز اليومية.

## ⚠️ ملاحظة هامة
التطبيق لا يحتاج إلى ملف `.env` أو اتصال بخادم. جميع البيانات مشفرة ومخزنة في متصفحك فقط. تأكد من عمل نسخة احتياطية دورية من صفحة "الإعدادات".
