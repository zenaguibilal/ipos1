# iPOS - Point de Vente Intelligent (Version Ultra-Clean)

**iPOS** هو تطبيق نقطة بيع (POS) متطور، يعمل بالكامل داخل المتصفح (Client-side) باستخدام تقنية **IndexedDB**. تم تنقية هذا المشروع بالكامل ليكون مثالياً من حيث الأداء ونظافة الكود.

## 🚀 البنية التقنية النهائية

*   **Framework:** Next.js 14 (App Router)
*   **Base de Données:** IndexedDB (via Dexie.js) - محلي 100% وآمن تماماً.
*   **Gestion d'état:** Zustand (مع Persistance في LocalStorage) لإدارة السلال والملف الشخصي.
*   **UI:** ShadCN UI & Tailwind CSS (Dark Mode فاخر).
*   **Offline-first:** التطبيق يعمل بدون إنترنت تماماً بعد التحميل الأول.

## 📁 هيكلية المشروع الرشيقة

- `src/app/`: المسارات والصفحات (لوحة التحكم، البيع، المنتجات، العملاء...).
- `src/services/`: منطق الأعمال المعزول (Sales, Stocks, Customers, Expenses, Bread).
- `src/lib/db.ts`: تعريف قاعدة البيانات المحلية.
- `src/stores/`: إدارة الحالة العالمية (App Store, Cart Store).
- `src/components/`: مكونات واجهة المستخدم المُنظمة.

## ✨ المميزات الرئيسية المستقرة

1.  **نظام البيع السريع:** دعم السلال المتعددة، التحقق من المخزون لحظياً، وإضافة مواد مخصصة.
2.  **إدارة المخزون المتقدمة:** تتبع دقيق للحركات (Inventory Logs) وتنبيهات بانتهاء الصلاحية.
3.  **الديون والعملاء:** سجل كامل لكل عميل مع كشوفات حساب احترافية قابلة للطباعة.
4.  **نظام الخبز:** إدارة فريدة للطلبات اليومية المتكررة وتحويلها لعمليات بيع بضغطة زر.
5.  **الأمان والنسخ الاحتياطي:** نظام تصدير واستيراد البيانات يدوياً لضمان ملكيتك الكاملة لبياناتك.

## 🗑️ التنظيف المطبق
تمت إزالة جميع التبعيات غير المستخدمة (مثل Radix Tabs)، وحذف الأنماط والرسوم المتحركة الزائدة لضمان أسرع وقت استجابة ممكن. المشروع الآن خالٍ تماماً من "الكود الميت".