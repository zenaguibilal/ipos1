# iPOS - Point de Vente Intelligent (Version Locale & Privée)

**iPOS** هو تطبيق نقطة بيع (POS) متطور، يعمل بالكامل داخل المتصفح (Client-side) دون الحاجة لخادم أو اتصال بالإنترنت. يستخدم تقنية **IndexedDB** لضمان سرعة فائقة وخصوصية مطلقة لبياناتك.

## 🚀 البنية التقنية (النسخة النهائية النظيفة)

*   **Framework:** Next.js 14 (App Router)
*   **Base de Données:** IndexedDB (via Dexie.js) - *تخزين محلي 100%*
*   **Gestion d'état:** Zustand (مع persistance في LocalStorage)
*   **UI:** ShadCN UI & Tailwind CSS
*   **Architecture:** Offline-first / No-Backend

## 📁 هيكل المشروع النظيف (Clean Architecture)

- `src/app/`: المسارات والصفحات (عامة بالكامل).
- `src/services/`: منطق الأعمال والتعامل مع IndexedDB.
- `src/lib/db.ts`: تعريف جداول قاعدة البيانات المحلية.
- `src/stores/`: إدارة حالة السلة (التطبيقات المتعددة) وحالة التطبيق.
- `src/components/`: مكونات واجهة المستخدم (التصميم الفاخر).

## ✨ المميزات الرئيسية

1.  **البيع السريع (POS):** واجهة متطورة تدعم السلال المتعددة (Brouillons) والتحقق من المخزون لحظياً.
2.  **إدارة المخزون:** تتبع دقيق، تنبيهات المخزون المنخفض، وتاريخ انتهاء الصلاحية.
3.  **الديون والعملاء:** نظام كامل لإدارة حسابات العملاء، حدود الائتمان، وكشوفات الحساب القابلة للطباعة.
4.  **نظام الخبز:** وحدة فريدة لإدارة الطلبات اليومية المتكررة للخبز.
5.  **الأمان والنسخ الاحتياطي:** تصدير واستيراد قاعدة البيانات كملف JSON لضمان عدم ضياع البيانات.

## ⚠️ ملاحظة هامة
التطبيق لا يتطلب ملف `.env`. جميع البيانات مشفرة ومخزنة في متصفحك فقط. تأكد من عمل نسخة احتياطية دورية من صفحة "الإعدادات".
