// Deprecated: Logic moved to root src/app/layout.tsx to avoid Next.js parallel layouts conflict.
export default function DeprecatedLayout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
