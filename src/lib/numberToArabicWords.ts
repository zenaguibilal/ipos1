/**
 * @fileOverview Ce fichier a été neutralisé. La conversion en langue arabe n'est plus supportée 
 * conformément aux exigences d'uniformisation linguistique intégrale en français.
 */
export function numberToArabicWords(amount: number): string {
    // Fonction désactivée pour maintenir la cohérence linguistique du projet.
    return "";
}
